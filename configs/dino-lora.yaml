session: dino_lora

data:
  ms1m_dir: data/MS1M-ArcFace
  num_identities: 85742
  val_fraction: 0.2

backbone:
  name: dino
  embedding_dim: 768
  dropout: 0.0
  lora:
    rank: 8
    alpha: 8
    targets: ["fc1", "fc2"]

loss:
  name: infonce
  temperature: 0.07

training:
  epochs: 15
  batch_size: 780
  lr: 0.001
  momentum: 0.9
  weight_decay: 0.0005
  warmup_epochs: 5
  num_workers: 6
  mixed_precision: fp16
  eval_every: 5
